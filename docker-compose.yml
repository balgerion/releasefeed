services:
  releasefeed:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./static:/app/static:ro
      - ./state.json:/app/state.json